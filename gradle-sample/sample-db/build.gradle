plugins {
    id 'java'
}
sourceCompatibility = JavaVersion.VERSION_1_8

dependencies {
    annotationProcessor project(':db')
    testImplementation("junit:junit:4.12")
}
compileJava {
    options.compilerArgs += "-Aconf=${projectDir}/src/conf"
}